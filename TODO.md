# TODO

# TODO (актуализация)

- Авторизация бота: `/auth <token>` по списку `BOT_ACCESS_TOKENS` из `.env`, `user_id` сохраняется в БД (user_auth).
- Управление токенами iTick — через `/itick` (токены в БД, без `.env`).
- Пресеты сигналов:
  - CRUD через `/signals`, лимит 3 на пользователя.
  - Пресет: имя → TF → сигналы (≥1) → фильтры (0..N).
  - Хранение списков id сигналов/фильтров в SQLite.
- Мониторинг:
  - `/start` → пара → выбор пресетов (если есть) или подсказка настроить через `/signals`.
  - Запуск мониторинга по каждому выбранному пресету с его TF/фильтрами/сигналами.
- Сообщения об ошибках iTick: формулировка про превышение лимита запросов.
